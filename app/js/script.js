$(document).ready(() => {
	$('.shows').selectize({
		placeholder: 'select a show'
	});
	$('.seasons').selectize({
		placeholder: 'select a season'
	});
	$('.episodes').selectize({
		placeholder: 'select an episode'
	});
});